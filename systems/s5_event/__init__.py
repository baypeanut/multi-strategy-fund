"""S5 - event-driven 8-K reaction-drift sleeve (forward shadow book).

The FIRST double-validated edge candidate the fund produced (RESEARCH_LOG E18:
8k-drift, lockbox calendar-time Sharpe 1.47, NW t=2.40). S5 trades it forward
LIVE on paper with ZERO further optimization - the true out-of-sample test that
also dissolves the survivorship and regime-concentration caveats going forward.

Disabled by default (config systems.s5_event.enabled). Flipping it live starts
an irreversible forward-test clock; that is a deliberate human action.
"""
